{
  "name": "onchain-monitor",
  "version": "0.1.0",
  "private": true,
  "description": "A deterministic on-chain activity tracker with governance alerts and anomaly checks.",
  "scripts": {
    "format": "prettier --write '**/*.{ts,sol,json,md}'",
    "build": "npx tsc -p tsconfig.json",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "ethers": "^5.6.9",
    "typescript": "^5.0.4",
    "ts-node": "^10.9.1",
    "dotenv": "^16.0.3",
    "hardhat": "^2.15.0"
  },
  "dependencies": {
    "@types/node": "^18.16.3"
  }
}
